import { RoomCreateRequest } from './room.create-request';

describe('RoomCreateRequest', () => {
  it('should create an instance', () => {
    // expect(new RoomCreateRequest()).toBeTruthy();
  });
});
